#python
def reverse_string(s):
    return s[::-1]

input_string = "Hello, world!"
reversed_string = reverse_string(input_string)
print("Chuỗi gốc:", input_string)
print("Chuỗi đảo ngược:", reversed_string)

Chuỗi gốc: Hello, world!
Chuỗi đảo ngược: !dlrow ,olleH

#csharp

using System;

class Program
{
    static void Main()
    {
        int[] numbers = { 3, 5, 7, 9 };
        int sum = 0;

        foreach (int number in numbers)
        {
            sum += number;
        }

        Console.WriteLine(sum);
    }
}

Tổng các số trong mảng: 24


-- quản trị trong phòng học: tắt tiếng, loại thành viên
-- chia sẻ tài liệu
-- chia sẻ màn hình
-- ghi chú
-- bảng trắng
-- duyệt thành viên